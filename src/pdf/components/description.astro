---
import BaseDescription from '@/components/description.astro';

export interface Props {
  content: string;
}

const { content } = Astro.props;
const baseClass = /* tw */ 'text-base font-normal text-gray-500';
---

<BaseDescription content={content} classList={[baseClass]} />
